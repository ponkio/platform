apiVersion: v1
kind: Service
metadata:
  name: {{ include "github_producer.fullname" . }}
  labels:
    {{- include "github_producer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "github_producer.selectorLabels" . | nindent 4 }}
